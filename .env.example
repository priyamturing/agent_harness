# Copy this file to `.env` and fill in the appropriate credentials.
# Only set the variables you need for the provider you will use.

OPENAI_API_KEY=your-openai-key
ANTHROPIC_API_KEY=your-anthropic-key
XAI_API_KEY=your-xai-key
OPENROUTER_API_KEY=your-openrouter-key

# Optional headers recommended by OpenRouter (used for rankings/analytics)
# OPENROUTER_HTTP_REFERER=https://your-app.example.com
# OPENROUTER_APP_TITLE=My MCP Benchmark Runner

# Optional provider routing controls (comma-separated slugs)
# OPENROUTER_PROVIDER_ONLY=fireworks,groq
# OPENROUTER_ALLOW_FALLBACKS=true

# Optional toggle for the beta Responses API
# OPENROUTER_USE_RESPONSES_API=false
